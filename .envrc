# direnv configuration for keto-tracker
# This ensures nvm is loaded when entering the directory

# Load nvm if available
if [ -s "$HOME/.nvm/nvm.sh" ]; then
    source "$HOME/.nvm/nvm.sh"
fi

# Use .nvmrc version if it exists
if [ -f ".nvmrc" ]; then
    nvm use
fi

